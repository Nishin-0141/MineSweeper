# 🚀 1. プロジェクトルート作成
```
mkdir minesweeper-app
cd minesweeper-app
```

# 🚀 2. フロントエンド（Next.js）作成コマンド

Next.js のプロジェクトは create-next-app で自動生成できます。
```
npx create-next-app@latest frontend
```

質問に沿って Yes/No を選ぶだけで OK。
（TypeScript は Yes 推奨）

# 🚀 3. Go バックエンド（Lambda 用ディレクトリ）作成
## 📁 フォルダ構成を一気に作成
```
mkdir -p backend/cmd/ranking
mkdir -p backend/internal/{db,ranking}
```

## 📦 Go module を初期化
```
cd backend
go mod init minesweeper-backend
cd ..
```

## 🔧 Lambda Handler の最低限の雛形を作成（任意）
```
cat << 'EOF' > backend/cmd/ranking/main.go
package main

import (
	"context"
	"encoding/json"

	"github.com/aws/aws-lambda-go/events"
	"github.com/aws/aws-lambda-go/lambda"
)

type Rank struct {
	Name string `json:"name"`
	Time int    `json:"time"`
}

func handler(ctx context.Context, req events.APIGatewayProxyRequest) (events.APIGatewayProxyResponse, error) {

	data := []Rank{
		{"Alice", 32},
		{"Bob", 45},
	}

	body, _ := json.Marshal(data)

	return events.APIGatewayProxyResponse{
		StatusCode: 200,
		Body:       string(body),
		Headers:    map[string]string{"Access-Control-Allow-Origin": "*"},
	}, nil
}

func main() {
	lambda.Start(handler)
}
EOF
```

※ AWS Lambda を使う場合は、
```
go get github.com/aws/aws-lambda-go/lambda
go get github.com/aws/aws-lambda-go/events
go get github.com/aws/aws-sdk-go-v2/aws
go get github.com/aws/aws-sdk-go-v2/config
go get github.com/aws/aws-sdk-go-v2/service/dynamodb
```

も必要です。

# 🚀 4. infra ディレクトリ作成

Terraform / CDK / SAM どれでも対応できるよう空フォルダだけ作っておきます。
```
mkdir -p infra/{terraform,cdk,sam}
```

# 🚀 5. docs ディレクトリ（任意）
```
mkdir docs
```
